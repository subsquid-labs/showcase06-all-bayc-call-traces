manifestVersion: subsquid.io/v0.1
name: showcase06-all-bayc-call-traces
version: 1
description: |-
  The very first evm squid from manifest
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "sqd", "process:prod" ]
  api:
    cmd: [ "sqd", "serve:prod" ]
